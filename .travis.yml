language: rust
rust: 
  - stable

cache:
  cargo

addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
    packages:
    - libboost-dev
    - clang

script:
  - cargo run --release --bin config
  - cargo run --release --bin transaction

env:
  global:
  - RUST_BACKTRACE=1
  - CC="clang"
